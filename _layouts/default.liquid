<!DOCTYPE html>
<html lang="en">
<head>
    <title>{% if page.title %}{{ page.title }} | {% endif %}{{ meta.title }}</title>

    {% include meta %}
    {% include css %}

</head>
<body>

    {{ content }}
    
</body>
</html>